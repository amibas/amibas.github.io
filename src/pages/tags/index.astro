---
import BaseLayout from '../../layouts/BaseLayout.astro';
import TagList from '../../components/TagList.astro';
import { getAllPosts, getAllTags } from '../../utils/posts';

const posts = await getAllPosts();
const tags = getAllTags(posts);
---

<BaseLayout title="全部标签 - 我的博客" description="博客全部标签">
  <section class="max-w-5xl mx-auto px-6 py-16 md:py-20">
    <div class="flex items-center gap-3 mb-8 animate-fade-in-up">
      <span class="w-1.5 h-1.5 rounded-full bg-accent-secondary" aria-hidden="true"></span>
      <h1 class="text-2xl md:text-3xl font-semibold tracking-wide text-text-primary">全部标签</h1>
    </div>
    <div class="animate-fade-in-up stagger-1">
      <TagList tags={tags} />
    </div>
  </section>
</BaseLayout>
